/** Switch admin data tables between paged footer and infinite scroll (loads more as you scroll). */
export type AdminTableDisplayMode = "pagination" | "infinite";

export const ADMIN_TABLE_DISPLAY_MODE: AdminTableDisplayMode = "pagination";
